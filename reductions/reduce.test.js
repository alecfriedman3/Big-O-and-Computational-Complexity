const expect = require('chai').expect;
// const algorithms = require('./algorithms')

describe('The Algorithm', () => {

  describe('for searching an array', () =>{

    it ('should return an array', () => {
      // expect(algorithms.search()).to.be.an('array')
    })

    it ('should test', () => {

    })

  })

})
